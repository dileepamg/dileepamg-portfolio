/**
 * The underlined inline link.
 *
 * The About section sets the prose around its links in running sentences, so
 * the treatment lives here rather than being written out at each mention and
 * drifting apart the first time one is edited.
 */
export const inlineLink =
  "text-ink hover:text-brand-text underline underline-offset-4 transition-colors";
